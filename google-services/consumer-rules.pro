# TelemetryDeck KotlinSDK Google Services — consumer ProGuard rules
#
# Intentionally minimal. This module exposes extension functions over
# public Google Play Billing types (BillingConfig, Purchase, ProductDetails).
# Play Billing ships its own consumer-rules.pro; this module adds no
# additional reflection, serialization or class-name dependencies.
